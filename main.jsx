import React from "react";
import ReactDOM from "react-dom/client";
import NomadLocal from "./NomadLocal";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NomadLocal />
  </React.StrictMode>
);
